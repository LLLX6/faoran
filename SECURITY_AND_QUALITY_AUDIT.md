# تدقيق الأمان والجودة لتطبيق خدماتي

- تاريخ التدقيق: 2026-07-18
- النطاق: الواجهة، خادم Python، SQLite، Data Adapter، LocalStorage/SessionStorage، Service Worker، Manifest، الطلبات، المحادثات، الإشعارات، المزودون، الإدارة، الاشتراكات، الدفع، OTP ورفع الملفات.
- المنهج: مراجعة يدوية، اختبارات إساءة استخدام مع خادم وقاعدة بيانات معزولين، اختبارات مجال وAPI وواجهة هاتف، Bandit، pip-audit، فحص أسرار وسلامة SQLite.

## النتائج والإصلاحات

| الخطورة | المشكلة وطريقة حدوثها | الموضع المتأثر | التأثير | الإصلاح المطبق | اختبار الإصلاح | الحالة |
|---|---|---|---|---|---|---|
| حرجة | كانت بيانات الحالة العامة أوسع من حاجة الزائر، ما قد يسمح بوصول معلومات إدارية أو شخصية عند طلب bootstrap دون جلسة. | `server.py`، بناء استجابة bootstrap العامة | كشف بيانات مزودين أو طلبات أو إعدادات لزائر غير مصرح له. | أُنشئت استجابة عامة مصغرة حسب الدور؛ حُذفت الهواتف والوثائق والإعدادات المالية والإدارية وهوية أصحاب الطلبات، وقُرّبت إحداثيات العرض العامة. | `tests/security-api.py`: `public_data_minimization` و`smoke-api.py`: `public_privacy` و`visitor_isolation`. | تم الإصلاح |
| حرجة | كانت بعض وسائط الوثائق والصور الخاصة قابلة للطلب عبر مسار رفع عام. | `server.py:1023-1057`, `server.py:2401-2469`, `server.py:2711-2753` | كشف وثائق التحقق أو صور الطلب والمحادثة دون تفويض. | صُنفت الوسائط الخاصة حسب المالك والنوع، ومُنعت من `/uploads/`، وأصبحت تُعرض بروابط HMAC موقعة قصيرة العمر مع فحص الجلسة والملكية. | `tests/security-api.py`: `signed_private_media` واختبار الرابط المباشر والمنتهي. | تم الإصلاح |
| عالية | جلسات المصادقة لم تكن تُعاد مراجعتها دائمًا من قاعدة البيانات، وقد تستمر بعد إيقاف الحساب أو تغيير الرمز. | `server.py:1360-1435` | استخدام جلسة قديمة بعد إيقاف الحساب أو استرجاع الرمز. | تخزين الجلسات وتدقيقها في SQLite بكل طلب، دعم الإلغاء وتاريخ الانتهاء، وإلغاء جلسات الحساب عند تغيير الرمز أو الحذف أو الاسترجاع. | `tests/security-api.py`: `session_revocation` و`inactive_account_revocation`. | تم الإصلاح |
| عالية | إمكانية تعديل/حذف عضو فريق أو فرع عبر معرف تابع لمزود آخر. | `server.py:4650-4740`, `server.py:5209-5290` | IDOR وتغيير بيانات شركة أو مزود آخر. | فرض `provider_id` في كل قراءة وتحديث، رفض عدم تطابق المالك، والتحقق من وجود المزود قبل عمليات الإدارة. | `tests/security-api.py`: `team_and_branch_idor`. | تم الإصلاح |
| عالية | رمز المالك كان قابلًا للتأثر بصلاحيات مدير الفريق في بعض المسارات. | `server.py:4612-4680` | استيلاء مدير على حساب مالك المزود. | منع المدير من إدارة المالك أو مدير آخر، وتقييد الصلاحيات بحسب الدور، وإلغاء الجلسات بعد تغيير الرمز. | `tests/security-api.py`: `provider_owner_pin_protection`. | تم الإصلاح |
| عالية | الاعتماد على اسم الملف وMIME المرسل دون التحقق الكامل من توقيع الملف. | `server.py:131-140`, `server.py:2401-2469` | رفع ملف متنكر قد يؤدي إلى محتوى ضار أو تخزين غير آمن. | تحقق magic bytes للصور وPDF والصوت والفيديو، حد للحجم، أسماء عشوائية، ورفض SVG والأنواع غير المدعومة. | `tests/security-api.py`: `mime_signature_validation`. | تم الإصلاح |
| عالية | رموز الجلسة والحالة الخاصة وطابور عدم الاتصال كانت قابلة للبقاء في LocalStorage بين الأدوار. | `index.html:638-780`, `index.html:992-1042` | تسرب جلسة المستخدم أو إشعاراته للزائر أو لحساب آخر على الجهاز نفسه. | نقل الرموز والحالة الخاصة إلى SessionStorage، حفظ حالة عامة منقحة فقط، ربط الطابور بهوية الجلسة، ومسح البيانات الخاصة عند 401/الخروج/الدخول كزائر. | `smoke-api.py`: `visitor_isolation` و`smoke-ui.js`: `visitorIsolation`. | تم الإصلاح |
| عالية | كان Service Worker قادرًا على الاحتفاظ باستجابات API أو الوسائط الخاصة. | `service-worker.js:1-78` | بقاء بيانات خاصة في cache وعرضها بعد تبديل الحساب. | منع cache لكل `/api/` و`/media/` و`/uploads/` والطلبات خارج الأصل، واستخدام `no-store`، ورفع إصدار cache. | `tests/security-api.py`: `service_worker_private_cache_block`. | تم الإصلاح |
| عالية | انتقالات حالة الدفع كانت تسمح بانتقالات غير منطقية بعد الدفع أو الاسترداد. | `khadamati_domain.py:1073-1225` | تفعيل اشتراك بعد استرداد أو تعديل حالة مالية بصورة غير صحيحة. | آلة انتقالات صريحة، منع الرجوع من paid إلى failed/cancelled، منع استرداد غير المدفوع، وتأكيد يدوي idempotent. | `tests/test_domain.py`: `test_payment_amount_and_webhook_are_server_verified`. | تم الإصلاح |
| عالية | محاولات الدخول لم تكن مقيدة بشكل موحد لكل الأدوار. | `server.py:73-75`, `server.py:1740-1800`, مسارات الدخول | تخمين رمز المستخدم أو المزود أو الإدارة. | قفل مؤقت قابل للضبط، عداد موحد، رسالة آمنة، إشعار أمني للإدارة، ومسح العداد بعد النجاح. | `tests/security-api.py`: `login_lockout`. | تم الإصلاح |
| متوسطة | أحجام JSON لم تكن محددة حسب المسار، ما يسمح باستهلاك ذاكرة زائد. | `server.py:2652-2677` | إبطاء أو تعطيل الخادم بطلب ضخم. | حدود إجمالية وحدود أصغر لمسارات الدخول، ورفض مبكر بـ413 قبل التحليل. | `tests/security-api.py`: `request_size_limit`. | تم الإصلاح |
| متوسطة | قيم الإدارة المالية والفروع والفريق كانت تقبل أنواعًا غير صحيحة أو مزودًا غير موجود، وقد تنتج 500 أو بيانات يتيمة. | `server.py:4930-5290`, `server.py:5356-5385` | فساد بيانات أو أخطاء خادم عند المدخلات غير المتوقعة. | أعداد محدودة ونهائية، booleans صارمة، تحقق الهاتف والموقع والمالك والدور والصلاحيات، و404/400 مفهومة. | py_compile، unit tests، API smoke وsecurity API. | تم الإصلاح |
| متوسطة | سياق SQLite كان ينهي المعاملة دون ضمان إغلاق الاتصال. | `server.py:143-153` | تسرب مقابض الملفات وأخطاء قفل SQLite تحت الضغط أو أثناء النسخ الاحتياطي. | تحويل `db()` إلى context manager يغلق الاتصال دائمًا بعد commit/rollback. | تشغيل 9 اختبارات المجال بلا Traceback، وAPI smoke بقاعدة معزولة. | تم الإصلاح |
| متوسطة | CORS ورؤوس حماية المتصفح لم تكن مقيدة بما يكفي. | `server.py:2590-2640` | توسيع سطح XSS/clickjacking أو قراءة أصل غير موثوق. | allowlist للأصول، CSP، HSTS في الإنتاج، `nosniff`، `frame-ancestors`, Referrer وPermissions Policy. | `tests/security-api.py`: `cors_allowlist`; فحص الاستجابات. | تم الإصلاح |
| متوسطة | استعلامات SQL متغيرة في التصدير وبعض المسارات سببت مخاطرة وصعوبة إثبات السلامة. | `export_khadamati_data.py`, `khadamati_domain.py` | احتمال حقن عند توسعة القوائم أو تغيير المدخلات مستقبلًا. | استعلامات ثابتة أو whitelist صريحة للجداول والحالات. | Bandit: صفر نتائج في 6,575 سطرًا منطقيًا. | تم الإصلاح |
| منخفضة | عنوان WhatsApp API كان مبنيًا بصورة ديناميكية من إعدادات البيئة. | `server.py`, دالة إرسال WhatsApp | إعداد بيئة خبيث قد يوجّه الطلب إلى مضيف غير مقصود. | تثبيت المضيف الرسمي والتحقق من إصدار API ومعرف الرقم. | مراجعة يدوية وBandit. | تم الإصلاح |
| منخفضة | اشتراك Push كان يقبل endpoint ناقصًا أو غير HTTPS. | `server.py:4354-4384` | بيانات اشتراك تالفة وفشل إرسال متكرر. | فرض HTTPS و`p256dh` و`auth` وحدود طول قبل الحفظ. | اختبار مسار API والتحقق اليدوي. | تم الإصلاح |
| منخفضة | حالات مراجعة/شكوى غير صحيحة كانت تُصحح بصمت أو تنجح لمعرف غير موجود. | `server.py`, مسارات جودة الإدارة | تضليل الإدارة وحفظ حالة غير مقصودة. | رفض صريح بـ400 و404 وإعادة حساب الجودة فقط لسجل موجود. | اختبار مسارات الإدارة وpy_compile. | تم الإصلاح |
| عالية | الضغط داخل حقول تسجيل الدخول كان يُورَّث إلى خلفية النافذة ويغلقها قبل إدخال الرمز. | `index.html`, `public/index.html`, معالج `onClick` و`openModal` | منع المستخدم أو المزود من إكمال الدخول على الهاتف. | قصر إغلاق الخلفية على الضغط المباشر عليها، وفصل إغلاق الطبقة عن الرجوع العام، وإضافة اختبار متصفح يلمس الحقل ويرسل النموذج الناقص ويتأكد أن النافذة باقية. | `tests/smoke-ui.js`: مسار الدخول على 320 و390 و1280 بكسل. | تم الإصلاح |
| متوسطة | المحادثة احتوت تحديثًا يدويًا ولم تسمح بإلغاء التسجيل الصوتي قبل الإرسال. | `index.html`, `public/index.html`, محادثة الطلب | تأخر الرسائل وزحام شريط الكتابة وإرسال تسجيل غير مقصود. | تحديث تلقائي كل 5 ثوانٍ أثناء فتح المحادثة فقط، إيقافه عند الإغلاق، وزر إلغاء مدمج للتسجيل الصوتي. | `tests/smoke-ui.js`: التحديث التلقائي، توقف المؤقت، إلغاء الصوت ثم إعادة تسجيله وإرساله. | تم الإصلاح |
| متوسطة | سياسة المحتوى منعت إطار خريطة OpenStreetMap ووسائط HTTPS عند تشغيل الواجهة من خادم Python. | `server.py`, رؤوس CSP | خريطة أو فيديو فارغ في نسخة الخادم رغم عمل الملف الثابت. | السماح المحدد لإطار OpenStreetMap ووسائط HTTPS مع بقاء حظر المصادر الأخرى. | اختبار الواجهة من خادم Python الفعلي على 390×844. | تم الإصلاح |

## نتائج الاختبارات

| الفحص | النتيجة |
|---|---|
| `python -m py_compile server.py khadamati_domain.py export_khadamati_data.py` | نجح |
| `python -m unittest discover -s tests -p "test_*.py" -v` | 14/14 نجحت |
| `python tests/security-api.py` | 11/11 ضوابط إساءة استخدام نجحت |
| `python tests/smoke-api.py` على خادم وقاعدة معزولين | 13/13 رحلات API نجحت |
| `node tests/smoke-ui.js` | نجح على 320×740 و390×844 و1280×900، ونجح مجددًا من خادم Python الفعلي |
| `node tests/performance-ui.js` | الجاهزية التفاعلية 1,339ms، وFCP عند 460ms، ضمن الهدف المحلي |
| Bandit | صفر نتائج قابلة للاستغلال؛ SQL الخاص بالترحيل محمي بتحقق صارم للمعرفات والتعريفات مع توثيق B608 |
| pip-audit | لا توجد ثغرات معروفة في `requirements.txt` |
| فحص الأسرار الملتزمة | لم تُكتشف مفاتيح خاصة أو tokens حقيقية |
| `PRAGMA integrity_check` | `ok` |
| `PRAGMA foreign_key_check` | صفر مخالفات |

## إجراءات خارجية قبل الاستخدام العام

هذه ليست أخطاء كود يمكن إخفاؤها، بل متطلبات تشغيل:

1. **التخزين الدائم:** خطة Render المجانية لا تضمن بقاء SQLite أو مجلد uploads. يلزم Persistent Disk أو PostgreSQL مع تخزين ملفات دائم قبل استقبال بيانات حقيقية.
2. **الأسرار:** يجب تعيين `KHADAMATI_ADMIN_CODE` و`KHADAMATI_OTP_PEPPER` و`KHADAMATI_MEDIA_SIGNING_KEY` بقيم قوية وفريدة في بيئة الاستضافة، لا داخل GitHub.
3. **OTP:** لا يصبح إنتاجيًا حتى ربط مزود تسليم رسمي وضبط مفاتيحه؛ لا توجد رموز تجريبية مقبولة في production.
4. **الدفع:** الوضع `manual` يبقي الطلب بانتظار الإدارة. الدفع الآلي يحتاج بوابة فعلية وتوقيع Webhook صحيح؛ لا يُعرض نجاح وهمي.
5. **Push:** الإشعارات الخارجية تحتاج مفاتيح VAPID وHTTPS وموافقة المستخدم؛ الإشعارات الداخلية تعمل دونها.
6. **مراقبة التشغيل:** يوصى بتفعيل نسخ احتياطية مشفرة، مراقبة أخطاء، معدل طلبات على مستوى reverse proxy، وسياسة تدوير للأسرار.

## ملاحظة منهجية

لا يوجد تدقيق يثبت انعدام الثغرات مستقبلًا. هذا التقرير يثبت نطاق الفحص والضوابط التي اختُبرت في هذا الإصدار، ويجب إعادة التدقيق عند إضافة بوابة دفع، مزود OTP، تخزين سحابي أو تغيير جوهري في الصلاحيات.
